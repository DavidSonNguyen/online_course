import 'dart:math';

import 'package:onlinelearning/features/states.dart';
import 'package:redux/redux.dart';

// void testAction(Store<AppState> store) async {
//   final searchResults = await new Future.delayed(
//     new Duration(seconds: 1),
//         () => Random().nextInt(10000000).toString(),
//   );
//
//   store.dispatch(searchResults);
// }
